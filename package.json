{
  "dependencies": {},
  "scripts": {
    "test": "mocha",
    "lint": "eslint src",
    "cover": "istanbul cover _mocha",
    "pretest": "npm run --silent lint"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "chai": "^3.4.1",
    "codeclimate-test-reporter": "^0.1.1",
    "eslint": "^1.9.0",
    "istanbul": "^0.4.0",
    "mocha": "^2.3.3"
  }
}

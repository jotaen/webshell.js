language: bash
sudo: required
services:
  - docker

script: make test

after_success:
  - make release

addons:
  artifacts:
    paths:
      - $(ls ./dist/*.js | tr "\n" ":")
      - $(ls ./dist/*.css | tr "\n" ":")
      - $(ls ./examples/* | tr "\n" ":")
    target_paths:
      - webshell.js
